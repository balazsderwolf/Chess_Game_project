package com.company.characterTests;

import com.company.characters.Karakter;
import com.company.characters.Paraszt;
import com.company.game_engine.Game;
import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class ParasztTest {
    /*Game game = new Game();
    private final Paraszt paraszt = new Paraszt("feher");


    @Test
    void testMovebackward() {
       assertFalse(paraszt.validateRequestedMove(6,0,7,0, game.getTable(), "feher"));
    }
    @Test
    void testMoveleft(){
        assertFalse(paraszt.validateRequestedMove(6,0,6,1, game.getTable(), "feher"));
    }
    @Test
    void testMoveForeward(){
        assertTrue(paraszt.validateRequestedMove(6,0,5,0, game.getTable(), "feher"));
    }
    @Test
    void testMoveattack(){
        assertFalse(paraszt.validateRequestedMove(6,0,5,1, game.getTable(), "feher"));
    }*/
}